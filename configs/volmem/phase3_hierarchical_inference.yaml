# 多尺度推理配置
# 粗→中→精三层 → 预期 2x 加速

model_name: 'MemFlowDiT'
inference_mode: 'hierarchical'

# 三层设置
coarse_scale: 3    # 间隔3取1
medium_scale: 2    # 间隔2取1
fine_scale: 1      # 全处理

# 保持其他参数
chunks_per_step: 12
memory_capacity: 4
