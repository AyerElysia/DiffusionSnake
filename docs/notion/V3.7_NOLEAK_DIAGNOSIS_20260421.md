# V3.7 no-leak 泛化掉点诊断（2026-04-21）

## 结论

这次 `btcv_diffusion_dit_v3_7_v6b_full_noleak` 的掉点，**不是训练没收敛**，而是“正式 no-leak 主线并没有真正复现 standalone V6b 的关键机制”。

### 已确认的核心问题

1. **`scale conditioning` 在正式主线里没有真正生效**
   - 配置里虽然开了 `v3_7_use_scale_conditioning: true`
   - 但 `FlowMatchingEvolution.predict_velocity()` 之前没有把 `contour_scale` 传给 denoiser
   - 结果：full no-leak 训练/推理时，V6b 最关键的尺度条件分支实际上是空跑的

2. **正式主线缺少 V6b 的 per-contour normalization**
   - standalone V5b/V5f/V6/V6b 都用了：`x1_norm = x1_raw / contour_scale`
   - 正式主线之前仍然走的是全局 `diffusion_disp_stats` 归一化
   - 对多器官、多尺寸轮廓来说，这会弱化复杂小结构和后续 contour 的细节恢复

3. **正式主线缺少 V6b 的采样策略**
   - standalone V6b：`t ~ Beta(0.3, 1.0)` + `zero_x0_prob=0.5`
   - 正式主线之前：`t ~ Uniform(0,1)` + 全随机高斯 `x0`
   - 这会让训练目标更平均，但也更容易得到“平滑但保守”的解

## 证据

### 1. 当前 full no-leak 结果

`visual/v3_7_eval_now_full_v6b_20260421_132756/v3_7_full_test_iou_20260421_132756.json`

- sample mean IoU: **0.8867**
- median: **0.8892**
- std: **0.0260**

相比旧版正式线：

`visual/v3_7_eval_now_full_20260421_132614/v3_7_full_test_iou_20260421_132614.json`

- sample mean IoU: **0.6650**

说明 **V6b 方向本身是有效的**，但还没把“无毛刺”和“高精度”同时拉满。

### 2. 训练日志说明不是没学会

`data/outputs/btcv_diffusion_dit_v3_7_v6b_full_noleak/logs.jsonl`

- 起始 loss: `0.1213`
- 当前 loss: `0.0013`
- 最近 50 step 平均: `0.00206`

训练损失已经很低，所以问题更像：

- 学到了**过于保守/平滑**的目标
- 而不是“训练还没收敛”

### 3. 图像现象

从 worst samples（如 `idx_012`, `idx_052`）看：

- 毛边基本没有了
- 大轮廓通常能对上
- 掉点主要来自：
  - 小结构偏移
  - 局部边界被抹平
  - 个别 contour 明显收缩/错位

这与“尺度信息没真正进入网络 + 目标归一化过粗”高度一致。

## 已完成修复

### 代码修复

已在 `lib/networks/diffusion/flow_matching_evolution.py` 中补上：

1. **contour_scale 计算与传递**
   - train / inference 都会根据当前 contour 计算 span
   - 会真正传给 `DiTFlowMatchingV3_7(..., contour_scale=...)`

2. **可选 per-contour normalization**
   - 新开关：`v3_7_use_contour_norm`
   - 开启后训练目标改为：`x1_raw / contour_scale`
   - 推理输出再乘回 `contour_scale`

3. **V6b-style FM sampling**
   - 新开关：`flow_zero_x0_prob`
   - 新开关：`flow_t_beta_alpha`, `flow_t_beta_beta`
   - 这样正式主线也能用上 Beta t-sampling 和 zero-x0 mix

### 新配置

新增：

- `configs/btcv_diffusion_dit_v3_7_v6c_full_noleak.yaml`

它是在正式 no-leak 主线里，**真正把 V6b 有效机制接回去** 的版本：

- `v3_7_use_scale_conditioning: true`
- `v3_7_use_contour_norm: true`
- `flow_zero_x0_prob: 0.5`
- `flow_t_beta_alpha: 0.3`
- `flow_t_beta_beta: 1.0`

## 下一步建议（优先级）

### 第一优先级
先跑 `v6c_full_noleak`

因为这一步不是新想法，而是把 **已经被 single-sample 证明有效的机制，真正落到正式主线**。

### 第二优先级
如果 `v6c` 仍然“无毛刺但偏保守”，再做两条分支：

1. **regularized-per-point 版本**
   - shared + delta
   - 目标：保留 per-point 解耦能力，同时提升泛化

2. **频域细节保真版本**
   - 开启 `v3_7_spectral_loss_k`
   - 提高高频项权重
   - 目标：抑制 burr 的同时，不把真实边界细节也抹掉

## 进展补充（2026-04-21 下午）

- 已用 `configs/btcv_diffusion_dit_v3_7_v6c_full_noleak_resume.yaml` 从
  `btcv_diffusion_dit_v3_7_v6b_full_noleak/checkpoints/latest.pt`
  成功续训 `v6c_full_noleak`
- 训练输出目录：
  `data/outputs/btcv_diffusion_dit_v3_7_v6c_full_noleak`
- 已确认不是“假启动”：
  - `launch.log` 已进入 `Starting epoch-based training loop`
  - `logs.jsonl` 已连续写到 `step 21053`
- 当前刚恢复后的 `diff_loss` 大约在 `0.042 ~ 0.055` 区间，说明修复后的正式主线已经在稳定跑 step

这意味着：后续可以直接基于这条正式 `v6c` 线继续观察 full no-leak 的真实泛化上限，而不是再怀疑主线代码没有真正生效。

## 新结论修正：`v6c` 不能直接从旧 `v6b` checkpoint 硬切

后续实际验证表明，上面的判断只说明 **`v6c` 主线代码跑通了**，但**不代表它适合直接从旧 `v6b_full_noleak` checkpoint 续训**。

### 已验证的 `v6c` 失败现象

使用：

- `configs/btcv_diffusion_dit_v3_7_v6c_full_noleak_resume_quicksave.yaml`
- checkpoint：`data/outputs/btcv_diffusion_dit_v3_7_v6c_full_noleak/checkpoints/epoch_357.pt`

在 `BtcvVal` 上 full val 的结果是：

- `mean_iou_sample_avg = 0.419090`

对应文件：

- `visual/v3_7_eval_now_full_v6c_epoch357/v3_7_full_test_iou_20260421_143936.json`

这说明一个关键事实：

> **把 `contour_norm + Beta-t + zero-x0 + scale-conditioning 真生效` 一次性叠到旧 `v6b` checkpoint 上，会产生严重的训练分布错位。**

也就是说，`v6c` 的问题不是“代码又没生效”，而是 **resume 策略本身不兼容**。

## 当前最稳的可用解：`v6d compat`

基于上面的失败结果，后面改成了 **分阶段迁移**：

1. **先保留旧 `v6b_full_noleak` 的训练目标分布不变**
   - 不启用 `v3_7_use_contour_norm`
   - 不引入 `flow_zero_x0_prob`
   - 不引入 `flow_t_beta_alpha/beta`
2. **只让“正式主线里真正生效的 scale conditioning”单独上线**
   - 即：继续用旧的全局 displacement normalization
   - 但现在 `contour_scale` 会真实传给 denoiser

新增配置：

- `configs/btcv_diffusion_dit_v3_7_v6d_full_noleak_resume_compat.yaml`

新增统一 full-val 评估脚本：

- `scripts/eval_v37_full_iou.py`

### `v6d compat` 实测结果

checkpoint：

- `data/outputs/btcv_diffusion_dit_v3_7_v6d_full_noleak_compat/checkpoints/epoch_357.pt`

full val：

- `visual/v3_7_eval_now_full_v6d_compat_epoch357/v3_7_full_test_iou_20260421_144501.json`

结果：

- `mean_iou_sample_avg = 0.8877465`

对比旧版 `v6b_full_noleak`：

- 旧：`0.8867439`
- 新：`0.8877465`
- 提升：`+0.0010026`

进一步比较：

- 150 个样本里，**83 个样本提升**
- 已知 hard case 中：
  - `idx 12`: `0.8098 -> 0.8271`（明显改善）
  - `idx 52`: `0.8421 -> 0.8477`
  - `idx 79`: `0.8406 -> 0.8413`
  - `idx 80`: `0.8372 -> 0.8165`（变差）
  - `idx 138`: `0.8425 -> 0.8392`（轻微变差）

## 现在的策略判断

当前最合理的路径不是继续直接推 `v6c`，而是：

> **先走 `v6d compat` 这种“低分布偏移”的兼容增强路线，把 scale-conditioning 的收益单独吃到，再考虑二阶段地引入 contour-norm / Beta-t / zero-x0。**

换句话说，下一阶段更适合做：

1. 继续基于 `v6d compat` 观察 full-training 是否还能继续稳定抬升
2. 若稳定，再做 staged migration：
   - stage A: `scale conditioning only`

## `v6d` 细节失配专项跟进：不是“再多训一点”就会好

用户后续指出：`visual/v3_7_eval_now_full_v6d_compat_epoch357` 里，整体平滑区域贴合不错，但复杂“拐来拐去”的局部细节仍然差，尤其是 `idx_013`。

### 已验证结论

对比：

- `v6d epoch357`
- `v6d latest(~epoch395)`
- `v6g warm-start`

结果表明：

- `idx_013` 从 `epoch357 -> epoch395` **没有实质性修复**
- 训练 loss 也已经很低，不像“再训久一点自然会好”
- 因此这个问题主要是 **表征/结构偏置**，不是单纯 epoch 不够

### 先前两条 loss-only 路线都不够

已经试过：

1. `v6e`：spectral/high-frequency loss
2. `v6f`：curvature-weighted point loss

结论：

- 都没有明显修复 `idx_013` 的复杂局部细节
- 说明问题不是简单的“再给高频更大 loss 权重”就能解决

## `v6g`：detail-context 结构分支的最新结论

为了解决上面的 detail underfit，新增了一个 **resume-safe detail-context 分支**：

- 文件：
  - `lib/networks/diffusion/dit_denoiser_v3_7.py`
  - `lib/networks/diffusion/flow_matching_evolution.py`
- 配置：
  - `configs/btcv_diffusion_dit_v3_7_v6g_full_noleak_resume_detailctx.yaml`
  - `configs/btcv_diffusion_dit_v3_7_v6g_full_noleak_detailctx_finetune.yaml`
  - `configs/btcv_diffusion_dit_v3_7_v6g_full_noleak_detailctx_only.yaml`

核心做法：

- 沿 contour 法向做多偏移 feature sampling
- 构造局部 detail 特征后送入 zero-init 的 detail projection
- 这样旧 checkpoint 加载时几乎不改变原行为，可以安全续训

### warm-start 验证

`v6g` 直接加载 `v6d epoch395` 的前 14 个样本切片几乎不变：

- `v6g warm-start`: `0.8721709`
- `v6d latest`: `0.8721631`

说明新分支初始化时是近似 identity 的，**结构接入本身安全**。

### 关键新发现：旧 resume 策略会给新分支“假阴性”

最初直接按旧方式续训 `v6g` 时，看起来几乎没有改善。进一步排查后确认：

- 旧 checkpoint 会把 `optimizer + scheduler + step` 一起恢复
- `v6d epoch395` 此时学习率已经在 cosine 尾段，只有 `~4e-8`
- 对 zero-init 的新 detail 分支来说，这等于几乎没有真正训练

因此新增了训练逻辑开关：

- `resume_weights_only: true`

对应代码：

- `diffusion_train.py`

含义：

- 只加载模型权重
- 不恢复 optimizer / scheduler / global step
- 让新结构从 fresh LR 重新开始学习

### `v6g` 两种真实训练方式的结果

#### 1. 整网一起 fine-tune（不理想）

配置：

- `configs/btcv_diffusion_dit_v3_7_v6g_full_noleak_detailctx_finetune.yaml`

`epoch1` 前 14 样本切片：

- `0.8695179`（低于 `v6d latest` 的 `0.8721631`）

`idx_013`：

- `mean_iou: 0.8551857 -> 0.8538863`
- 最差 contour（最后一条）：
  - `0.6726727 -> 0.6451613`

结论：

- **整网一起调会先破坏原稳定基线**
- 不适合拿来判断 detail 分支本身有没有用

#### 2. 只训练 detail 分支（更干净，也更有信息量）

配置：

- `configs/btcv_diffusion_dit_v3_7_v6g_full_noleak_detailctx_only.yaml`

在 `diffusion_train.py` 中新增：

- `train.detail_context_only: true`

作用：

- 只训练
  - `detail_local_proj`
  - `detail_point_proj`
- 实测训练参数量：
  - `230,400 / 14,839,558`

##### epoch1

- 14 样本切片：`0.8712007`
- 比整网 fine-tune 稳，但仍略低于基线

`idx_013`：

- `0.8551857 -> 0.8522095`
- 最差 contour：
  - `0.6726727 -> 0.6578171`

##### epoch3

- 14 样本切片：`0.8721833`
- 已经 **略高于** `v6d latest` 的 `0.8721631`

但 `idx_013` 仍然没有真正修好：

- `0.8551857 -> 0.8534516`
- 各 contour delta：
  - `c0: +0.001998`
  - `c1: -0.000470`
  - `c2: -0.000416`
  - `c3: +0.002142`
  - `c4: +0.001196`
  - `c5: -0.014856`

也就是说：

> `detail-context` 分支对一批样本和 `idx_013` 的部分 contour 有轻微正增益，**但没有击中 `idx_013` 最难的最后一条复杂 contour**。

### 当前判断

到目前为止，可以比较清楚地说：

1. `v6d` 的 detail failure **不是单纯“再训久一点”**
2. `normal-direction detail-context` 这个方向 **不是完全没用**
   - 它能在不破坏整体切片均值的情况下，给部分样本带来小幅提升
3. 但它 **还不足以解决用户最关心的 hardest contour**
   - `idx_013` 的最后一条复杂 contour 依然是主要瓶颈

因此下一步更值得考虑的，不是继续盲目拉 epoch，而是针对这类 contour 继续改结构，例如：

- 加入 **tangent-direction / along-curve context**
- 改善复杂 contour 的 **point parameterization / resampling**
- 或者对 hardest contour 引入更强的局部几何约束
   - stage B: 再单独引入 `contour_norm`
   - stage C: 最后再评估 `Beta-t / zero-x0`

## 关键判断

这次掉点的本质不是：

- “V6b 思路错了”
- 或者“scale conditioning 没价值”

而是：

> **正式 no-leak 训练并没有真正跑到 standalone V6b 的方法本体。**

所以当前最值得做的，不是盲目换网络，而是先把 **V6b 的有效机制完整迁回正式主线**，再看它在 full no-leak 下的真实上限。

## 2026-04-21 晚间补充：修正 curve 分支冻结失真后的真实结论

### 1. 关键实验偏差：之前的 `v6i` 没有真正训练到 curve 分支

后续复查发现，`diffusion_train.py` 里的 `train.detail_context_only` 最初只放开了：

- `detail_local_proj`
- `detail_point_proj`

没有包含：

- `detail_curve_local_proj`
- `detail_curve_point_proj`

直接检查 `v6i epoch1 / epoch5 / epoch9` checkpoint 后确认：

- `detail_curve_local_proj.2.weight`
- `detail_curve_point_proj.2.weight`

在这些 checkpoint 中都仍然是 **全 0**。

这说明：

> 之前把 `v6i` 的收益归因到 along-curve neighborhood context 是不严谨的；  
> 它本质上还是在继续训练旧的 detail 分支，而不是新 curve 分支真的学起来了。

### 2. 已修复的训练与结构控制

这轮已经补上：

- `diffusion_train.py`
  - `detail_context_only` 现在会真正包含 `detail_curve_*`
  - 新增 `train.detail_context_head_only: true`
    - 允许只训练 **detail 分支 + final output head**
- `lib/networks/diffusion/dit_denoiser_v3_7.py`
- `lib/networks/diffusion/flow_matching_evolution.py`
  - 新增 `v3_7_detail_curve_inject_mode`
  - 支持：
    - `both`
    - `local`
    - `point`

这样后面的 curve-context 结果才是可信的。

### 3. `v6j`：真正训练起来的 curve+head 分支

配置：

- `configs/btcv_diffusion_dit_v3_7_v6j_full_noleak_detailctx_curve_head.yaml`

特点：

- 从 `v6i epoch5` 做 weights-only resume
- 训练：
  - `detail_local_proj`
  - `detail_point_proj`
  - `detail_curve_local_proj`
  - `detail_curve_point_proj`
  - `final_layer`

启动日志确认：

- trainable params = `1,248,256 / 15,659,782`

并且 checkpoint 权重确认：

- `detail_curve_*` 的末层权重已经不再是 0
- 说明这次 curve 分支确实在学

#### `v6j epoch5`

- 14-sample slice mean = `0.872743`
- `idx_013 mean_iou = 0.855914874`
- `idx_013`
  - `c4 = 0.738170347`
  - `c5 = 0.670658683`

对比 `v6d epoch357`：

- `idx_013 mean_iou = 0.856372214`
- `c4 = 0.774671053`
- `c5 = 0.670694864`

结论：

> 真正训练起来的 curve-context **对最难的 `c5` 是有帮助的**，已经几乎回到 `v6d` 水平；  
> 但它会明显伤到 `c4`，所以整体上还不是最终可用解。

### 4. `v6k`：只把 curve-context 注入到 point stream

为了解决 `v6j` “curve 对 `c5` 有益，但 local 注入会拖坏 `c4`” 的问题，新增：

- `configs/btcv_diffusion_dit_v3_7_v6k_full_noleak_curve_point_head.yaml`

关键设置：

- `v3_7_detail_curve_inject_mode: 'point'`
- 不再把 curve-context 注入 `local_ctx`
- 只注入 `x / point stream`

#### `v6k epoch1`

- 14-sample slice mean = **`0.873074`**
  - 是当前 detail 结构线里最好的 14-sample slice mean
- full-val (`BtcvVal`, 150 samples):
  - `visual/v3_7_eval_now_full_v6k_epoch1/v3_7_full_test_iou_20260421_193635.json`
  - `mean_iou_sample_avg = 0.889628`

对比：

- `v6i epoch5 full-val = 0.889366`
  - 提升：`+0.000262`
- `v6d epoch357 full-val = 0.8877465`
  - 提升：`+0.0018815`

但在用户最关心的难样本上：

- `v6k epoch1`
  - `idx_013 mean_iou = 0.853085071`
  - `c4 = 0.737007874`
  - `c5 = 0.666666667`
- `v6k epoch2`
  - `idx_013 mean_iou = 0.853088340`
  - `c4 = 0.746411483`
  - `c5 = 0.661721068`

结论：

> `point-only curve injection` 对 **整体泛化** 是当前最优方向；  
> 但它仍然**没有击中 `idx_013` 这类极端 tortuous detail**。

### 5. 当前判断

到这里可以更清楚地分成两个目标：

1. **如果目标是主线泛化**
   - 当前最好结果是：
     - `v6k epoch1 full-val = 0.889628`
   - 它已经超过：
     - `v6i epoch5 = 0.889366`
     - `v6d epoch357 = 0.8877465`

2. **如果目标是专门修 `idx_013` 这种复杂弯折局部**
   - 继续堆 curve-context 已经出现明显边际递减
   - 下一个更可能击中的方向应转向：
     - **point resampling / parameterization**
     - 或 **curvature-adaptive point allocation**

也就是说：

> 现在最像“真正更好主线”的是 `v6k`；  
> 但要彻底解决用户指出的那类最难细节，下一步更应该改 **点参数化**，而不是继续单纯堆 context 分支。

## 2026-04-21 深夜补充：runtime 采样 bug 修复 + `v6l` 参数化实验

### 1. 修掉了一个真实的 runtime 参数化 bug

在 `lib/utils/snake/snake_gcn_utils.py::uniform_upsample()` 的 PyTorch fallback 里，原来对 `k>1` 的边使用了：

- `torch.linspace(..., steps=k)[:-1]`

这会导致：

- 每条边实际只生成 `k-1` 个点
- 最后再用尾点重复 padding 补齐到 `p_num`

也就是说运行时存在：

> **少采样 + 尾点重复补齐**

这是一个真实 bug，已经修复为：

- `torch.linspace(..., steps=k+1)[:-1]`

不过后续复测表明：

- 这个 bug **不是当前 `eval_v37_full_iou.py` 指标变化的主导来源**
- 修复后现有 `v6d / v6k` 指标与 `idx_013` 基本不变

结论：

> 这是必须修的正确性问题，但**不是本轮 detail failure 的主因**。

### 2. 新方向：固定 128 点下的 curvature-aware 重采样（`v6l`）

既然 curve-context 已经出现边际递减，本轮继续往“点参数化”走了一步，但先不做 variable-length 大改，而是先做：

> **固定 128 点，按曲率重加权分配采样密度**

修改：

- `lib/utils/snake/snake_config.py`
  - 新增：
    - `poly_resample_mode`
    - `poly_resample_curvature_alpha`
- `lib/utils/snake/snake_voc_utils.py`
  - `uniformsample()` 新增可选 curvature-weighted edge allocation
- `lib/utils/snake/snake_gcn_utils.py`
  - `uniform_upsample()` 新增相同的 curvature-weighted edge allocation
  - 同时修复上面的 runtime sampling bug

新增配置：

- `configs/btcv_diffusion_dit_v3_7_v6l_full_noleak_curve_point_resample.yaml`

关键设置：

- 基于 `v6k` 的 point-only curve injection
- 额外开启：
  - `poly_resample_mode: 'curvature'`
  - `poly_resample_curvature_alpha: 1.5`

### 3. `v6l` 的结果：对整体泛化是正增益，但不是 hardest-detail 解

#### 不重训，仅用 `v6k epoch1` checkpoint + `v6l` 参数化推理

- 14-sample slice:
  - `v6k epoch1 = 0.873074`
  - `v6l-from-v6k epoch1 = 0.873288`

说明：

> 仅从推理参数化层面看，curvature-aware resampling 对整体切片已经是正向的。

但 `idx_013` 反而更差：

- `v6k epoch1`
  - `mean = 0.853085071`
  - `c4 = 0.737007874`
  - `c5 = 0.666666667`
- `v6l-from-v6k epoch1`
  - `mean = 0.852576767`
  - `c4 = 0.735015773`
  - `c5 = 0.661721068`

这说明：

> 这条参数化变化 **需要重新训练适配**，不能只靠 inference 直接吃收益。

#### `v6l` 从 `v6k epoch1` 做 weights-only 训练后的结果

训练输出：

- `data/outputs/btcv_diffusion_dit_v3_7_v6l_full_noleak_curve_point_resample`

##### `epoch1`

- 14-sample slice = `0.873535`
- full-val = `0.889555`

##### `epoch2`

- 14-sample slice = **`0.873886`**（本轮参数化线最好）
- full-val = `0.889273`

##### `epoch3`

- 14-sample slice = `0.873518`

##### `epoch5`

- 14-sample slice = `0.873078`

趋势判断：

- `v6l` 的峰值出现在 `epoch2`
- 后面开始轻微回落

### 4. `v6l` 的定位结论

`v6l` 明确证明了：

> **点参数化 / 重采样方向对整体泛化是有效的。**

因为：

- `v6l epoch2` 的 14-sample slice = `0.873886`
- 高于：
  - `v6l epoch1 = 0.873535`
  - `v6k epoch1 = 0.873074`

但它仍然没有修好用户最关心的 hardest sample：

- `v6l epoch2`
  - `idx_013 mean = 0.852711170`
  - `c4 = 0.729264476`
  - `c5 = 0.660714286`
- 仍明显低于：
  - `v6d epoch357`
    - `mean = 0.856372214`
    - `c4 = 0.774671053`
    - `c5 = 0.670694864`

同时在 full-val 上：

- `v6k epoch1 = 0.889628`
- `v6l epoch1 = 0.889555`
- `v6l epoch2 = 0.889273`

所以当前主线排序仍然是：

1. **主线 full-val 最优：`v6k epoch1 = 0.889628`**
2. **参数化切片最优：`v6l epoch2 = 0.873886`**

### 5. 当前最终判断（截至本轮）

1. `curve-context` 不是没用，但已经很接近边际收益极限
2. `point resampling / parameterization` 方向 **对整体泛化有效**
3. 但固定 128 点下的曲率重采样，**仍然不足以修掉 `idx_013` 这种 hardest tortuous contour**

因此下一步真正值得做的，不再是继续叠：

- detail context
- curve context
- fixed-128 curvature resampling

而应该转到更强的结构变化：

> **adaptive point allocation / higher-point-count V3.7-FM**

也就是：

- 让困难 contour 真正拥有 **更多可用点数**
- 而不是只在固定 128 点里重新分配密度
