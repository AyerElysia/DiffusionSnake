# 毛刺问题分析工具 - 使用指南

## 快速开始

我已经为你创建了一套完整的毛刺分析工具。以下是使用步骤：

### 1. 测试工具是否正常工作

```bash
cd /mnt/sdb1/leijh/DiffusionSnake/DiffusionSnake-12-30
bash test_burr_analysis.sh
```

这会分析第0个样本，生成所有可视化结果。

### 2. 批量分析多个样本

```bash
bash run_burr_analysis.sh
```

这会分析样本0-4，生成完整的对比数据。

### 3. 查看结果

```bash
ls -lh visual/burr_analysis/
```

每个样本会生成5个文件：
- `burr_idx{N}_analysis.png` - 综合分析图（6个子图）
- `burr_idx{N}_original_order.png` - 原始点序可视化
- `burr_idx{N}_reordered.png` - 重排后的点序
- `burr_idx{N}_comparison.png` - 点序对比图
- `burr_idx{N}_metrics.json` - 所有统计指标

## 工具说明

### 核心功能

1. **散点图可视化**
   - 不按点序连线，直接显示点的分布
   - 如果散点图平滑但连线图有毛刺 → 点序问题
   - 如果散点图本身就乱 → 预测质量问题

2. **曲率热力图**
   - 用颜色显示每个点的曲率
   - 红色 = 高曲率（尖锐转角）
   - 蓝色 = 低曲率（平滑区域）

3. **点序跳跃检测**
   - 自动识别相邻点距离异常大的位置
   - 统计跳跃数量和位置
   - 可视化跳跃点

4. **最近邻重排**
   - 尝试用最近邻算法重新排列点序
   - 对比重排前后的改进效果
   - 量化跳跃减少数量

5. **统计指标**
   - 点间距离统计（均值、标准差、变异系数）
   - 曲率统计（均值、最大值、异常点数量）
   - 转角统计（尖锐转角数量）
   - 高频振荡检测（振荡点数量）

### 分析思路

根据老师的建议，我们重点关注：

**问题：毛刺是否由点序导致？**

**判断方法：**

1. **看散点图**
   - 打开 `burr_idx{N}_analysis.png` 的左上角子图
   - 如果点的分布看起来很规整 → 点本身没问题
   - 如果连线后出现毛刺 → 很可能是点序问题

2. **看点序跳跃**
   - 打开 `burr_idx{N}_original_order.png`
   - 橙色X标记的是跳跃点
   - 红色粗线是跳跃连线
   - 如果跳跃数量多（>10个） → 点序有严重问题

3. **看重排效果**
   - 打开 `burr_idx{N}_reordered.png`
   - 对比跳跃数量的变化
   - 如果重排后跳跃大幅减少 → 说明原始点序确实有问题

4. **看统计指标**
   - 打开 `burr_idx{N}_metrics.json`
   - 查看 `point_order_analysis.improvement.jump_reduction`
   - 如果这个值很大（比如从20减少到2） → 点序是主要问题

### 可能的结论

**情况A：点序导致的毛刺**
- 散点图平滑 ✓
- 连线图有毛刺 ✓
- 大量点序跳跃（>10个） ✓
- 重排后改善明显 ✓

**解决方案：**
- 检查 `lib/utils/snake/snake_gcn_utils.py` 中的点序生成逻辑
- 检查初始化八边形的点序
- 考虑在推理时使用最近邻重排

**情况B：预测质量问题**
- 散点图本身就很乱 ✓
- 点序跳跃少（<5个） ✓
- 重排后改善不明显 ✓

**解决方案：**
- 增加训练时的平滑损失
- 调整扩散步数
- 使用边缘感知平滑后处理

**情况C：混合问题**
- 既有点序跳跃，散点图也有噪声

**解决方案：**
- 同时优化点序和模型训练

## 设计的统计算法

我设计了以下算法来量化毛刺现象：

### 1. 点序跳跃检测算法

```python
# 计算相邻点距离
dists = ||p[i+1] - p[i]||

# 计算阈值
threshold = mean(dists) + 3 * std(dists)

# 检测跳跃
jumps = {i | dists[i] > threshold}
```

**意义：** 如果相邻点距离远大于平均值，说明点序可能有问题。

### 2. 曲率计算算法

```python
# 二阶差分
d1 = p[i+1] - p[i]
d1_prev = p[i] - p[i-1]
d2 = d1 - d1_prev

# 曲率
curvature = ||d2||
```

**意义：** 曲率大的点是尖锐转角，可能是毛刺。

### 3. 高频振荡检测算法

```python
# 计算点到前后连线的距离
line_vec = p[i+1] - p[i-1]
point_vec = p[i] - p[i-1]

# 叉积计算距离
deviation = |cross(point_vec, line_vec)| / ||line_vec||

# 检测高频点
high_freq = {i | deviation > mean + 2*std}
```

**意义：** 如果一个点偏离其前后连线很远，说明是高频振荡（毛刺）。

### 4. 最近邻重排算法

```python
# 贪心算法
visited = {0}
current = 0
order = [0]

for _ in range(n-1):
    # 找未访问点中距离最近的
    unvisited = {i | i not in visited}
    nearest = argmin{||p[i] - p[current]|| | i in unvisited}

    visited.add(nearest)
    order.append(nearest)
    current = nearest
```

**意义：** 如果重排后效果变好，说明原始点序有问题。

## 下一步建议

1. **先运行测试**
   ```bash
   bash test_burr_analysis.sh
   ```

2. **查看第一个样本的结果**
   - 打开 `visual/burr_analysis/burr_idx0_analysis.png`
   - 重点看散点图和点序跳跃

3. **根据结果决定方向**
   - 如果是点序问题 → 修改点序生成逻辑
   - 如果是预测质量问题 → 改进模型训练
   - 如果是混合问题 → 两方面都要改

4. **批量分析**
   ```bash
   bash run_burr_analysis.sh
   ```
   看看多个样本是否有共同的模式

## 文件清单

- `analyze_burr_scatter.py` - 主分析脚本
- `analyze_point_order.py` - 点序分析模块
- `edge_smoothing.py` - 平滑后处理模块（已有）
- `run_burr_analysis.sh` - 批量运行脚本
- `test_burr_analysis.sh` - 快速测试脚本
- `BURR_ANALYSIS_README.md` - 详细文档

## 注意事项

1. **使用V3.4模型**：脚本会自动查找 `btcv_diffusion_dit_v3_4_single_overfit` 的checkpoint

2. **不加后处理**：先看原始预测的散点图，然后再对比加了平滑后的效果

3. **环境要求**：必须在 `snake1` conda环境中运行

4. **GPU使用**：会自动使用可用的GPU，没有GPU会用CPU（较慢）

## 示例输出

运行后你会看到类似这样的输出：

```
============================================================
点序问题分析
============================================================

[1] 点序跳跃检测:
    总点数: 128
    平均点间距: 3.45
    标准差: 1.23
    最大点间距: 15.67
    检测到跳跃: 8 处
    跳跃位置: [12, 34, 56, 78, 90, 102, 115, 120]
    跳跃距离: ['12.34', '15.67', '11.23', ...]

[2] 重排后（最近邻）:
    平均点间距: 3.52
    标准差: 0.89
    最大点间距: 6.78
    检测到跳跃: 1 处

[3] 改进效果:
    跳跃减少: 7 处
    最大距离减少: 8.89
    标准差减少: 0.34
```

这说明原始点序有8个跳跃，重排后只剩1个，说明点序确实是问题所在。

## 联系

如有问题，请随时询问！
